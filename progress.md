# Progress Log

| CP | Date | Outcome |
|---|---|---|
| **CP-1** | 2026-06-28 | Skeleton + data + manifest layer green. `src/reference_date.py`, `src/parsing.py`, `src/io_utils.py`, `src/manifest.py` shipped. 50-candidate edge-case fixture in `tests/conftest.py`. 30/30 pytest green. ruff check + format clean. |
| **CP-2** | 2026-06-28 | All 9 feature builders shipped (`src/features/*`) + 6 config YAMLs (`config/*.yaml`) + `src/feature_pipeline.py` + `build_features.py` skeleton. 126/126 pytest green; ruff check + format clean. Smoke run on the 50-candidate fixture produces tier histogram `{A:18, B:1, C:14, D:9, E:8}` — all 3 honeypots drop, all 5 stuffers land in Tier E. |
| **CP-3** | 2026-06-28 | BGE-small-en-v1.5 vendored under `artifacts/model/` (~135 MB, gitignored). `src/embeddings.py` + `tools/vendor_model.py` shipped. Full `build_features.py` produced all 6 artifacts from 100K candidates in **64 min 5 s** wall-clock on M4 CPU (encoder dominates: 3670.8 s / 61.2 min for 100K × BGE-small at batch=512). `verify_manifest` passes against the produced manifest. Tier histogram `{A:82, B:384, C:10188, D:87275, E:2071}`; honeypot_drop=348 (audit=15143); rank_50 ceilings=41093. 138 pytest green (137 unit + new e2e). ruff check + format clean. |
| **CP-3.5** | 2026-06-28 | Encoder perf patch (no re-run of 100K). `src/embeddings.py` adds `resolve_device("auto")` (MPS on Apple Silicon, CPU otherwise). `build_features.py` exposes `--device` + `--torch-threads`; flips `TOKENIZERS_PARALLELISM` to `true`. Measured CPU + threads=8 + parallel tokenizers throughput: 33 docs/s (≈50 min for 100K, down from 64). MPS path rejected: batch=512 OOM, smaller batches stalled on per-kernel launch overhead. 140 pytest green. CP-3 artifacts still verify_manifest clean. |

| **CP-4** | 2026-06-28 | Online ranking + reasoning + audit shipped. `src/scoring.py`, `src/ranking.py`, `src/reasoning.py`, `rank.py` (with import allow-list static check), `reasoning_audit.py`, `config/skeletons.yaml`, 2 new feature builders (`experience_band`, `education`), `tools/rebuild_features_parquet.py`. Full 100K run: `rank.py` finishes in **1.96 s** wall on M4 (target 60 s, hard cap 300 s). `validate_submission.py` PASS on `top_100_submission.csv`. `reasoning_audit.py` PASS 100/100. Top-10 promotion pool: 275 eligible at min_must_have=2 (no relaxation). Rank-50 ceilings clipped 41,093 rows. 196 pytest green (138 existing + 58 new across scoring/ranking/reasoning/audit/imports/e2e + 2 feature). ruff check + format clean. |
| **CP-5a** | 2026-06-28 | Stratified holdout sourcing shipped. `tools/build_holdout.py` (9 buckets × 11 candidates, seed=42, refuse-overwrite guard), `tools/holdout_report.py` (9 predicate + 4 label-grounded assertions, vacuous-pass on empty buckets), `tests/test_holdout.py` (35 cases), `holdout_labels.csv` (99 rows blank for hand-fill), `holdout_report.md`. Bucket choices deviate from completed.md draft — substituted 3 vacuous buckets (CV-Speech-Expert, Strong-Recsys-Weak-Skill, Strong-AI-Inactive — archetypes never fire on real 100K) with `honeypot_audit`, `services_to_product`, `irrelevant_tail`. 8/9 predicate assertions PASS against current `top_100_submission.csv`; 1 near-miss: `plain_language median rank = 52` vs threshold ≤ 50. 231 pytest green; ruff clean. |
| **CP-5b** | 2026-06-28 | Ablation suite + runtime report shipped. `tools/ablations.py` (A0..A5 driver, deep-copied weights to prevent mutation leak, per-variant CSVs to gitignored `ablations/`), `tools/runtime_report.py` (subprocess `rank.py` + RSS via getrusage), `tests/test_ablations.py` (22 cases), `ablation_report.md`, `runtime_report.md`. Findings on real 100K: A3 (no behavioral mult) flips 7/10 top-10 ranks — `availability × logistics` is the dominant lever; A1 (no embedding) = 99/100 overlap (embedding ≈ noise for top-100); A4 + A5 = 100/100 (defensive only). `rank.py` wall = **1.87 s**; peak RSS = 1.8 GB; artifacts = 91.45 MB no model / 219.72 MB with model. 253 pytest green; ruff clean. |
| **CP-5c** | 2026-06-28 | HuggingFace Space / Docker sandbox shipped. `app.py` (Gradio entry, `RankerState` lazy singleton, `rank_sample()` reuses `src.scoring` + `src.ranking` + `src.reasoning` on fresh sample), `requirements-app.txt` (gradio sandbox-only), `Dockerfile` (Python 3.11-slim + model vendored at build time), `SANDBOX.md`, `tests/test_app.py` (11 cases). `app.py` is a SEPARATE entry point — gradio / sentence-transformers do NOT pollute `rank.py`'s allow-list. Smoke-verified end-to-end on 20-candidate real sample → 5 ranked rows with proper reasoning. 264 pytest green; ruff clean; rank.py allow-list test (4/4) still passes. |
| **CP-5d** | 2026-06-28 | Submission PPTX builder shipped. `tools/build_deck.py` (opens Redrob template via python-pptx, populates all 11 slides per `pptx.md` brief, renders slides 6 + 7 as shape diagrams, slide 8 pulls live numbers from `build_features_summary.json` + `top_100_audit.csv`), `tests/test_build_deck.py` (11 cases), `requirements-dev.txt` adds python-pptx. PICTURE shapes + title text boxes preserved; only prompt blocks rewritten. PDF export manual (libreoffice not installed locally). 275 pytest green; ruff clean. |

> Next: CP-S1 (floor submission) — verify all 11 `hld.md` blocking checks pass. Operator hand-steps before full sign-off: label `holdout_labels.csv`, export `redrob_submission.pdf`, push to GitHub, deploy HF Space, regen deck with final URLs.
