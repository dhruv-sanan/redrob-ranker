# Production runtime deps. rank.py import allow-list = {numpy, pandas, pyarrow, json, sys, pathlib, yaml}.
# Phase 3 adds sentence-transformers (offline-only, used by build_features.py — never imported by rank.py).
numpy>=1.26,<2.0
pandas>=2.2,<2.3
pyarrow>=15.0,<16.0
PyYAML>=6.0,<7.0
