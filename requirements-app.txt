# Sandbox-only deps. Layered on top of requirements.txt.
#   pip install -r requirements.txt -r requirements-app.txt
#
# rank.py NEVER imports anything from this file — only app.py.
gradio>=4.44,<5.0  # 4.44 fixes gradio_client.utils.get_type crash on additionalProperties: bool schema
